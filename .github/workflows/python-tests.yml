name: Python Tests

on:
  push:
    branches:
      - '4.0'
      - 'master'
  pull_request:
    branches:
      - '*'
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.7, 3.8]

    steps:
      - uses: actions/checkout@v2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: Build artifact
        run: |
          ./mvnw clean package -DskipTests -P spark-3.0 -P scala-2.12 --no-transfer-progress
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyspark==3.0.1 "testcontainers[neo4j]"
      - name: Test with pytest
        run: |
          cd ./scripts/python
          python test_spark.py "3.0" "2.12"